spring:
  datasource:
    url: jdbc:h2:mem:gestore-agenti-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  sql:
    init:
      mode: always
      schema-locations: ""
      data-locations: classpath:/data.sql

  flyway:
    enabled: true
    clean-disabled: false
    clean-on-validation-error: true
    locations: classpath:db/migration

  test:
    database:
      replace: none
